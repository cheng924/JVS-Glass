<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 圆角半径（数值越大越圆） -->
    <corners android:radius="10dp" />

    <!-- 背景颜色 -->
    <solid android:color="@color/white" />

    <!-- 边框（可选） -->
    <stroke
        android:width="1dp"
        android:color="@color/teleprompter_text" />
</shape>